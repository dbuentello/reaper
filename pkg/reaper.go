package reaper

// Version is a constant variable containing the version number for the reaper package
const Version = "0.1.0ALPHA"

// Build is the unique number based off the git commit in which it is compiled against
var Build = "nonRelease"
